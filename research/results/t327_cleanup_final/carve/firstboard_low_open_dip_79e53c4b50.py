\n","lineDetails":[{"lines":["1-53"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/strategies/two_board_pullback_dip.py","aiAddedLines":["1-305"],"aiDeletedLines":[],"aiModifiedContent":"\"\"\"2板回调低吸策略 (B2-A) - Task#9 引擎精测。\n\n形态(来自 Task#7 全维度网格复审, 研究口径见 data/realtime/task7_refine.txt):\n  恰2板(最近涨停日连板数==2, 第2板非一字) → 断板回调1~2天(期间不再涨停)\n  → 买入日竞价低开 -4% \u003c= open_rate \u003c 0 → H1_open 买入 → hold1 次日收盘卖出\n排序: 更低开优先(open_rate升序) — task7_top1_probe: top1 +0.91%/日, 2021-2026六年全正\n\n研究口径(池子级 hold1, 毛收益): n=5388 胜率48.4% 均值+0.67% 六年全正\n引擎精测(2021-01~2026-07-23, slot=1, 含成本): CAGR +56.21% | MDD 73.10%\n  | 641笔 胜率49.45% +0.65%/笔 | 分年 21:+70 22:-33 23:+3 24:+46 25:+194 26:+121\n  → 观察池(50-100%档); 详见 data/realtime/task9_engine_report.txt\nTask#12 干净数据重跑(stocks.db 2026-05-25~06-30缺口修复后): CAGR +61.36%,\n  2026 +121→+174, 其余年份不变; 卖点前移变体 two_board_pullback_dip_h1c\n  (D2 H1收盘卖+slot当小时复用) **CAGR +107.79% 达标** → 推荐替代本对照版。\n变体 B2-B(two_board_pullback_dip_b): 加\"昨日红盘占比 red_ratio \u003c 40\"过滤,\n  red_ratio 取 D0(昨日)值(D0收盘后即确定, 无未来数据); 缺失日显式跳过不买入。\nred_ratio 权威源=index_kline(sh.000001), 已补齐到 2026-07-23(Task#10 Robin修复)。\n\nTask#12 卖点前移(exit_mode 参数化, 买入逻辑不动):\n  hold1_close 现行对照: D2 hour4 收盘卖\n  d2h1_open / d2h1_close / trail_h1: D2 H1内了结 → slot当小时可复用\n依据: Lee时段结构(H1唯一系统性正时段) x Task#9归因(hold1占slot隔日才空)。\n\n卖出参数声明: hold1 策略无盘中止盈止损, 声明极宽 TP/SL 仅为满足框架读取要求,\n实际退出 = 买入次日 hour4 收盘(expired); 跌停封死顺延后尽快卖出(deferred)。\n\"\"\"\nimport sqlite3\n\nimport numpy as np\n\nimport trading_rules\nfrom strategies.base import Strategy, Signal, SellSignal\n\n_DB = '/home/AIWealth/data/stocks.db'\n\n\ndef _limit_flags(frame, codes):\n    \"\"\"返回 (valid, is_limit, is_yizi, close) 对齐 codes 的numpy数组。\n\n    涨停判定: close \u003e= 涨停价 - 0.001, 涨停价统一trading_rules.limit_prices\n    (Decimal ROUND_HALF_UP交易所口径, Task#299替换np.round银行家舍入旁路)。\n    一字板: open \u003e= 涨停价 - 0.001。ST股已在候选层排除, 此处用非ST比例。\n    \"\"\"\n    sub = frame.reindex(codes)\n    pre = sub['preclose'].to_numpy(dtype=float, na_value=0.0)\n    clo = sub['close'].to_numpy(dtype=float, na_value=0.0)\n    opn = sub['open'].to_numpy(dtype=float, na_value=0.0)\n    lp = np.array([trading_rules.limit_prices(c, p)[0]\n                   for c, p in zip(codes, pre)])\n    valid = (pre \u003e 0) \u0026 (clo \u003e 0)\n    is_lim = valid \u0026 (clo \u003e= lp - 0.001)\n    is_yizi = is_lim \u0026 (opn \u003e 0) \u0026 (opn \u003e= lp - 0.001)\n    return valid, is_lim, is_yizi, clo\n\n\ndef _lim_price(row, code):\n    \"\"\"单行涨停价(与_limit_flags同口径, Task#299统一trading_rules); Task#68 F2过滤用。\"\"\"\n    pre = float(row.get('preclose') or 0)\n    return trading_rules.limit_prices(code, pre)[0] if pre \u003e 0 else 0.0\n\n\ndef _is_lim(row, code):\n    lp = _lim_price(row, code)\n    c = float(row.get('close') or 0)\n    return lp \u003e 0 and c \u003e 0 and c \u003e= lp - 0.001\n\n\ndef _load_red_map() -\u003e dict:\n    \"\"\"{date: red_ratio(float)}; 缺失日不在map中, 调用方显式跳过。\"\"\"\n    out = {}\n    try:\n        conn = sqlite3.connect(_DB)\n        for d, r in conn.execute(\n                \"SELECT date, red_ratio FROM index_kline \"\n                \"WHERE code='sh.000001'\"):\n            if r is not None:\n                out[d] = float(r)\n        conn.close()\n    except sqlite3.Error:\n        pass\n    return out\n\n\nclass TwoBoardPullbackDipStrategy(Strategy):\n    \"\"\"B2-A: 恰2板回调1~2天 → 低开-4~0 → H1低吸 → 次日收盘卖出。\"\"\"\n\n    name = \"two_board_pullback_dip\"\n    max_hold_hours = 8           # D1 h1 买入 → D2 h4 收盘 = 8小时\n    sell_day_no_buy = True\n    buy_hour = 1\n\n    # === 核心参数(Task#7 网格最优) ===\n    open_rate_min = -4.0         # 竞价低开下限\n    open_rate_max = 0.0          # 竞价低开上限(不含)\n    red_ratio_max = None         # B2-B变体设为40.0; None=不启用红盘过滤\n    min_history_bars = 20        # 上市未满20根K线不买(对齐研究口径 nth\u003e=20)\n    # Task#63/68 F2板质量过滤: 第2板首触涨停hour \u003e 该值则剔除(1=只留H1早板);\n    # None=不过滤零侵入。逻辑自 scripts/t63_engine_runner.py T63Variant 转正,\n    # 特征全部来自板日(D-1..D-4)K线, D1开盘前可见, 时序合规\n    max_seal_hour_b2 = None\n\n    # hold1 无盘中TP/SL; 极宽声明仅为满足框架卖出参数读取要求(不会触发)\n    take_profit_pct = 9.99       # +999%, 实际不可达\n    stop_loss_pct = -0.99        # -99%, 实际不可达\n\n    # === Task#12 卖点前移 ===\n    # hold1_close(现行对照) | d2h1_open | d2h1_close | trail_h1\n    exit_mode = 'hold1_close'\n    trail_pp = 2.0               # trail_h1 回撤触发(pp), 基于D2 H1_open\n\n    def __init__(self):\n        self._cand_codes = []\n        self._cand_date = None\n        self._red_map = None\n\n    def _red_ratio(self, date: str):\n        if self._red_map is None:\n            self._red_map = _load_red_map()\n        return self._red_map.get(date)\n\n    def get_candidates(self, date, data_feed):\n        self._cand_codes, self._cand_date = [], date\n\n        # 回看5个交易日: d[0]=D0(昨日) .. d[4]\n        chain = []\n        d = date\n        for _ in range(5):\n            d = data_feed._prev_trading_date(d)\n            if not d:\n                return []\n            chain.append(d)\n\n        # B2-B: 昨日红盘占比过滤(D0收盘后已知); 缺失日显式跳过, 不引入未来数据\n        if self.red_ratio_max is not None:\n            red = self._red_ratio(chain[0])\n            if red is None or red \u003e= self.red_ratio_max:\n                return []\n\n        f_today = data_feed._load_day(date)\n        if f_today is None or f_today.empty:\n            return []\n        frames = [data_feed._load_day(d) for d in chain]\n        if any(f is None or f.empty for f in frames):\n            return []\n\n        codes = f_today.index\n        flags = [_limit_flags(f, codes) for f in frames]\n        v = [x[0] for x in flags]\n        L = [x[1] for x in flags]\n        Y = [x[2] for x in flags]\n\n        # 恰2板 + 回调1~2天(期间无涨停), 第2板非一字:\n        #   case1: D0未板, D-1/D-2连板, D-3未板   (断板后第1天)\n        #   case2: D0/D-1未板, D-2/D-3连板, D-4未板 (断板后第2天)\n        case1 = v[0] \u0026 v[1] \u0026 v[2] \u0026 v[3] \u0026 ~L[0] \u0026 L[1] \u0026 L[2] \u0026 ~L[3] \u0026 ~Y[1]\n        case2 = (v[0] \u0026 v[1] \u0026 v[2] \u0026 v[3] \u0026 v[4]\n                 \u0026 ~L[0] \u0026 ~L[1] \u0026 L[2] \u0026 L[3] \u0026 ~L[4] \u0026 ~Y[2])\n        pattern = case1 | case2\n\n        # 今日竞价窗口 + 基础池过滤\n        orate = f_today['open_rate'].to_numpy(dtype=float, na_value=np.nan)\n        opn = f_today['open'].to_numpy(dtype=float, na_value=0.0)\n        win = (orate \u003e= self.open_rate_min) \u0026 (orate \u003c self.open_rate_max)\n        # Task#36: signal模式(晚间候选生成)次日开盘未知, permissive开盘价(-20%)\n        # 落在带状窗口[-4,0)之外会误杀全部候选 → 跳过窗口过滤只筛形态;\n        # 9:25 live模式(真实开盘价注入)与回测(无_mode属性)窗口照常生效\n        if getattr(data_feed, '_mode', None) == 'signal':\n            win = np.ones(len(codes), dtype=bool)\n        not_bj = ~codes.str.startswith('bj.')\n        st = f_today['isST'].fillna(0).astype(int).to_numpy() \u003e 0\n        if 'code_name' in f_today.columns:\n            st = st | f_today['code_name'].fillna('').astype(str) \\\n                .str.upper().str.contains('ST').to_numpy()\n\n        mask = pattern \u0026 win \u0026 (opn \u003e 0) \u0026 not_bj \u0026 ~st\n        if not mask.any():\n            return []\n\n        # 排序: 更低开优先(open_rate升序)\n        idx = np.where(mask)[0]\n        idx = idx[np.argsort(orate[idx], kind='stable')]\n\n        final = []\n        for i in idx:\n            code = codes[i]\n            # 上市历史检查(对齐研究口径 nth\u003e=20), 仅对少量入围者查库\n            hist = data_feed.get_stock_history(code, date,\n                                               self.min_history_bars)\n            if len(hist) \u003c self.min_history_bars:\n                continue\n            final.append(code)\n\n        # Task#63/68 F2: 板质量过滤(第2板必须在max_seal_hour_b2内首触涨停);\n        # 与T63Variant完全同序同口径: 在基础候选链(含历史检查)之后叠加\n        if self.max_seal_hour_b2 is not None:\n            kept = []\n            for code in final:\n                br = self._board_rows(code, date, data_feed)\n                if br is None:\n                    continue                  # 板日重推失败: 保守剔除\n                _b1, b2 = br\n                if self._seal_hour(b2, code) \u003e self.max_seal_hour_b2:\n                    continue\n                kept.append(code)\n            final = kept\n\n        self._cand_codes = final\n        return final\n\n    def _board_rows(self, code, date, data_feed):\n        \"\"\"返回 (b1_row, b2_row) 或 None; 板日从回看链重推(与形态判定一致)。\"\"\"\n        chain = []\n        d = date\n        for _ in range(4):\n            d = data_feed._prev_trading_date(d)\n            if not d:\n                return None\n            chain.append(d)\n        frames = [data_feed._load_day(x) for x in chain]\n        if any(f is None or f.empty or code not in f.index for f in frames):\n            return None\n        # case1: 板=chain[1],chain[2]; case2: 板=chain[2],chain[3]\n        if _is_lim(frames[1].loc[code], code):\n            b2, b1 = frames[1].loc[code], frames[2].loc[code]\n        elif _is_lim(frames[2].loc[code], code):\n            b2, b1 = frames[2].loc[code], frames[3].loc[code]\n        else:\n            return None\n        return b1, b2\n\n    def _seal_hour(self, row, code):\n        \"\"\"首次触涨停时段(hourN_high\u003e=涨停价的最早N); 无hour数据返回0。\"\"\"\n        lp = _lim_price(row, code)\n        for h in (1, 2, 3, 4):\n            v = row.get(f'hour{h}_high')\n            if v is not None and np.isfinite(v) and v \u003e= lp - 0.001:\n                return h\n        return 0\n\n    def should_buy(self, code, date, hour, data_feed, portfolio):\n        if hour != self.buy_hour:\n            return None\n        if date != self._cand_date or code not in self._cand_codes:\n            return None\n        price = data_feed.get_hour_open(code, date, hour)\n        if not price or price \u003c= 0:\n            return None\n        # 双保险: 开盘已涨停不可买(引擎另有兜底)\n        limit_up, _ = data_feed.get_limit_prices(code, date)\n        if limit_up \u003e 0 and price \u003e= limit_up - 0.001:\n            return None\n        return Signal(code=code, price=float(price), strategy_name=self.name,\n                      target_hold_hours=self.max_hold_hours)\n\n    def should_sell(self, position, date, hour, data_feed):\n        # T+1: 买入当日不卖(框架另有兜底)\n        if position.buy_date \u003e= date:\n            return None\n        if self.exit_mode != 'hold1_close':\n            return self._sell_forward(position, date, hour, data_feed)\n        # hold1: 次日(D2) hour4 收盘卖出\n        if hour == 4:\n            price = data_feed.get_hour_close(position.code, date, 4)\n            if not price or price \u003c= 0:\n                price = data_feed.get_hour_open(position.code, date, 4)\n            if price and price \u003e 0:\n                return SellSignal(reason='expired', price=float(price))\n            return None\n        # 跌停封死顺延/停牌后的兜底: 已超过hold1目标仍持仓 → 尽快卖出\n        if position.hours_held \u003e= self.max_hold_hours:\n            price = data_feed.get_hour_open(position.code, date, hour)\n            if price and price \u003e 0:\n                return SellSignal(reason='deferred', price=float(price))\n        return None\n\n    def _sell_forward(self, position, date, hour, data_feed):\n        \"\"\"Task#12 前移变体: D2 H1内了结, 卖出当小时slot即可复用。\n        跌停封死/缺数据顺延 → 之后任意小时尽快开盘价卖出(deferred)。\"\"\"\n        code = position.code\n        if hour != 1:\n            price = data_feed.get_hour_open(code, date, hour)\n            if price and price \u003e 0:\n                return SellSignal(reason='deferred', price=float(price))\n            return None\n        o = data_feed.get_hour_open(code, date, 1)\n        c = data_feed.get_hour_close(code, date, 1)\n        lo = data_feed.get_hour_low(code, date, 1)\n        if not o or o \u003c= 0:\n            return None\n        if self.exit_mode == 'd2h1_open':\n            return SellSignal(reason='expired', price=float(o))\n        if self.exit_mode == 'd2h1_close':\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n        if self.exit_mode == 'trail_h1':\n            # 保守挂单语义: 触发价只基于H1_open, 不做bar内peak乐观假设\n            trig = o * (1 - self.trail_pp / 100)\n            if lo and lo \u003e 0 and lo \u003c= trig:\n                return SellSignal(reason='stop_loss', price=float(trig))\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n        return None\n\n    def get_buy_price(self, code, date, hour, data_feed):\n        return data_feed.get_hour_open(code, date, hour)\n","lineDetails":[{"lines":["1-30","32-39","42-47","50-55","68-96","101-163","169-192","207-209","240-305"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"},{"lines":["31","40-41","48-49","57","59"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"e66d27c3-d3c2-4f26-a172-7d07018434d9","userQueryBusinessId":"b18f83e8-8560-44f3-a478-c98127e22dce"},{"lines":["56","58","60-67","97-100","193-206","210-239"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"2e1ba1ac-3013-4fbb-8557-e44020720c88"},{"lines":["164-168"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"7443cdbf-7a38-4af2-b23f-53ce5c6bf833"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/tools/backfill_index_kline.py","aiAddedLines":["1-90"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"[Task #10] index_kline 缺行回补: 以 stock_kline 交易日为基准,\n补齐 sh.000001 缺失日期行(BaoStock拉取, 失败重试)。\n\nsz.399001 裁定更新[Task#295]: 已推翻废弃裁定恢复维护, 缺口回补与日常检测\n由 backfill_index_kline_tencent.py(腾讯源, 双指数)承担; 本工具仍仅补sh.000001。\n\n用法: python3 tools/backfill_index_kline.py\n\"\"\"\nimport math\nimport sqlite3\nimport time\n\nimport baostock as bs\n\nDB = '/home/AIWealth/data/stocks.db'\n\n\ndef safe_float(val):\n    if val is None or val == '':\n        return None\n    try:\n        v = float(val)\n        return None if math.isnan(v) or math.isinf(v) else v\n    except (ValueError, TypeError):\n        return None\n\n\ndef calc_rate(price, preclose):\n    if price is None or preclose is None or preclose == 0:\n        return None\n    return round((price / preclose - 1) * 100, 4)\n\n\ndef main():\n    conn = sqlite3.connect(DB)\n    missing = [r[0] for r in conn.execute(\n        \"SELECT DISTINCT s.date FROM stock_kline s \"\n        \"LEFT JOIN index_kline i ON i.date=s.date AND i.code='sh.000001' \"\n        \"WHERE i.date IS NULL ORDER BY s.date\")]\n    if not missing:\n        print(\"index_kline sh.000001 无缺行\")\n        conn.close()\n        return\n    print(f\"缺行: {missing}\")\n\n    lg = bs.login()\n    if lg.error_code != '0':\n        print(f\"BaoStock登录失败: {lg.error_msg}\")\n        conn.close()\n        return\n\n    filled = 0\n    for d in missing:\n        for attempt in range(3):  # 限流/断线重试\n            rs = bs.query_history_k_data_plus(\n                'sh.000001',\n                'date,code,open,high,low,close,preclose,volume,amount',\n                start_date=d, end_date=d, frequency='d')\n            rows = []\n            while rs.error_code == '0' and rs.next():\n                rows.append(rs.get_row_data())\n            if rows:\n                break\n            print(f\"{d}: 第{attempt + 1}次拉取为空/失败, 重试...\")\n            time.sleep(2)\n        if not rows:\n            print(f\"{d}: 3次重试仍失败, 跳过\")\n            continue\n        row = rows[0]\n        o, h, l, c, pre = [safe_float(x) for x in row[2:7]]\n        vol, amt = safe_float(row[7]), safe_float(row[8])\n        vals = [row[0], row[1], '上证综指', o, h, l, c, pre, vol, amt,\n                calc_rate(o, pre), calc_rate(h, pre),\n                calc_rate(l, pre), calc_rate(c, pre)]\n        vals.extend([None] * 40)  # hour1-4 占位\n        vals.append(None)         # red_ratio 由 update_red_ratio.py 生成\n        conn.execute(\n            'INSERT OR REPLACE INTO index_kline VALUES ('\n            + ','.join(['?'] * 55) + ')', vals)\n        conn.commit()  # 短事务, 避免长锁\n        filled += 1\n        print(f\"{d}: close={c} 已补\")\n    bs.logout()\n    conn.close()\n    print(f\"完成: 补{filled}/{len(missing)}行\")\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-4","7-90"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"},{"lines":["5-6"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"e66d27c3-d3c2-4f26-a172-7d07018434d9","userQueryBusinessId":"b18f83e8-8560-44f3-a478-c98127e22dce"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/strategies/firstboard_low_open_dip.py","aiAddedLines":["1-306"],"aiDeletedLines":[],"aiModifiedContent":"\"\"\"首板次日低开低吸策略 (FB-A) - Task#9 引擎精测。\n\n形态(来自 Task#7 全维度网格复审, 研究口径见 data/realtime/task7_refine.txt):\n  昨日(D0)首板涨停(连板数==1, 非一字) → 今日(D1)竞价低开 -4% \u003c= open_rate \u003c -2%\n  → 昨日红盘占比 red_ratio \u003e= 60(强势日错杀) → H1_open 买入\n  → 退出 tp+6% / sl-10%, 最长5个监控日(D6收盘兜底)\n排序: D0换手升序(低换手=筹码未松动优先) — task7_top1_probe:\n  top1 +1.50%/日 2021-2026 六年全正; 注意换手降序方向则失效(-0.31%)。\n\n研究口径(池子级 tp6sl-10, 毛收益): n=1574 胜率58.4% 均值+0.73% 5正1平\n引擎精测(2021-01~2026-07-15, slot=1, 含成本): CAGR +72.53% | MDD 44.81%\n  | 219笔 胜率65.75% +1.62%/笔 | 唯一负年 2023 -7.9% → 观察池第一顺位\nTask#12 干净数据+全区间重跑(至2026-07-23): CAGR +64.85%, 2026 +16.9→-7.8\n  (数据修复+区间延长), 2021-2025 分年不变; 卖点前移两变体(d2c/h1)引擎\n  精测均劣于本对照(+56.8%/+61.3%), FB-A **保持现行 tpsl_d6 口径不动**——\n  粗测代理曾示 d2c 更优(+86%), 引擎证伪: 变体笔数增加(226→332/447)但\n  均收益稀释(+1.47→+0.92/+0.74%/笔), slot=1 路径依赖下 2021/2024/2026\n  转弱, MDD 还升(44.8→45.0/55.7)。只信引擎数据。\nred_ratio 权威源=index_kline(sh.000001) red_ratio 列, 2026-07-23 前已补齐\n(Task#10 Robin 修复); 缺失日显式跳过不买入作为防御, 不引入未来数据。\n\nTask#12 卖点前移(exit_mode 参数化, 买入逻辑不动):\n  tpsl_d6   现行对照: tp6/sl-10 挂单监控 D2..D6, D6收盘兜底\n  tpsl_h1   挂单仅监控 D2 H1, 未触发 H1收盘强平 (D2起slot当小时可复用)\n  d2h1_open / d2h1_close / trail_h1  同 B2-A 语义\n依据: Lee时段结构(H1唯一系统性正时段) x Task#9归因(hold长占slot致信号捕获减半)。\n\"\"\"\nimport sqlite3\n\nimport numpy as np\n\nimport trading_rules\nfrom strategies.base import Strategy, Signal, SellSignal\n\n_DB = '/home/AIWealth/data/stocks.db'\n\n\ndef _load_red_map() -\u003e dict:\n    \"\"\"{date: red_ratio(float)}; 缺失日不在map中, 调用方显式跳过。\"\"\"\n    out = {}\n    try:\n        conn = sqlite3.connect(_DB)\n        for d, r in conn.execute(\n                \"SELECT date, red_ratio FROM index_kline \"\n                \"WHERE code='sh.000001'\"):\n            if r is not None:\n                out[d] = float(r)\n        conn.close()\n    except sqlite3.Error:\n        pass\n    return out\n\n\nclass FirstboardLowOpenDipStrategy(Strategy):\n    \"\"\"FB-A: 昨日首板 → 低开-4~-2 → 红盘\u003e=60 → D0低换手优先 → H1低吸。\"\"\"\n\n    name = \"firstboard_low_open_dip\"\n    max_hold_hours = 20          # 监控D2..D6, D6收盘兜底(同研究口径5日窗口)\n    sell_day_no_buy = True\n    buy_hour = 1\n\n    # === 核心参数(Task#7 网格最优) ===\n    open_rate_min = -4.0         # 竞价低开下限\n    open_rate_max = -2.0         # 竞价低开上限(不含)\n    red_ratio_min = 60.0         # 昨日红盘占比下限(D0收盘后已知)\n    min_history_bars = 20        # 上市未满20根K线不买(对齐研究口径 nth\u003e=20)\n    # Task#68: 晚间候选json保留数(覆盖全局CANDIDATE_TOP_N=20截断)。\n    # signal模式跳过开盘窗口过滤后形态池70-100+只且D0换手排序与次日\n    # 开盘无关, 截断即丢9:25复筛宇宙; 行情批量接口50只/请求可承受\n    candidate_top_n = 300\n\n    # 卖出参数显式声明(fixed模式)\n    take_profit_pct = 0.06       # 止盈 +6%\n    stop_loss_pct = -0.10        # 止损 -10%\n\n    # === Task#50 挖潜维度: D0封板时间过滤(默认None=不过滤, 零侵入) ===\n    # 'early'=H1/H2首次触板(早板筹码强), 'late'=H3/H4触板; 无hour数据按不过关处理\n    seal_filter = None\n\n    # === Task#12 卖点前移 ===\n    # tpsl_d6(现行对照) | tpsl_d2c(到期提前到D2收盘) | tpsl_h1 | d2h1_open\n    # | d2h1_close | trail_h1\n    exit_mode = 'tpsl_d6'\n    trail_pp = 2.0               # trail_h1 回撤触发(pp), 基于D2 H1_open\n    _H1_MODES = ('tpsl_h1', 'd2h1_open', 'd2h1_close', 'trail_h1')\n\n    def __init__(self):\n        self._cand_codes = []\n        self._cand_date = None\n        self._red_map = None\n\n    def _red_ratio(self, date: str):\n        if self._red_map is None:\n            self._red_map = _load_red_map()\n        return self._red_map.get(date)\n\n    def get_candidates(self, date, data_feed):\n        self._cand_codes, self._cand_date = [], date\n\n        d0 = data_feed._prev_trading_date(date)   # 昨日(首板日)\n        if not d0:\n            return []\n        dm1 = data_feed._prev_trading_date(d0)    # 前日(须未涨停)\n        if not dm1:\n            return []\n\n        # 情绪过滤: 昨日红盘占比 \u003e= 60; 缺失日显式跳过(不引入未来数据)\n        red = self._red_ratio(d0)\n        if red is None or red \u003c self.red_ratio_min:\n            return []\n\n        f_today = data_feed._load_day(date)\n        f_d0 = data_feed._load_day(d0)\n        f_dm1 = data_feed._load_day(dm1)\n        if any(f is None or f.empty for f in (f_today, f_d0, f_dm1)):\n            return []\n\n        codes = f_today.index\n\n        def limit_flags(frame):\n            sub = frame.reindex(codes)\n            pre = sub['preclose'].to_numpy(dtype=float, na_value=0.0)\n            clo = sub['close'].to_numpy(dtype=float, na_value=0.0)\n            opn = sub['open'].to_numpy(dtype=float, na_value=0.0)\n            # [Task#299] 涨停价统一走trading_rules.limit_prices(Decimal\n            # ROUND_HALF_UP交易所口径), 替换np.round银行家舍入旁路(t292审计P0)\n            lp = np.array([trading_rules.limit_prices(c, p)[0]\n                           for c, p in zip(codes, pre)])\n            valid = (pre \u003e 0) \u0026 (clo \u003e 0)\n            is_lim = valid \u0026 (clo \u003e= lp - 0.001)\n            is_yizi = is_lim \u0026 (opn \u003e 0) \u0026 (opn \u003e= lp - 0.001)\n            return valid, is_lim, is_yizi\n\n        v0, lim0, yizi0 = limit_flags(f_d0)\n        v1, lim1, _ = limit_flags(f_dm1)\n\n        # 昨日首板(非一字) + 前日未涨停(=恰好第1板)\n        pattern = v0 \u0026 v1 \u0026 lim0 \u0026 ~yizi0 \u0026 ~lim1\n\n        # 今日竞价窗口 + 基础池过滤\n        orate = f_today['open_rate'].to_numpy(dtype=float, na_value=np.nan)\n        opn = f_today['open'].to_numpy(dtype=float, na_value=0.0)\n        win = (orate \u003e= self.open_rate_min) \u0026 (orate \u003c self.open_rate_max)\n        # Task#68: signal模式(晚间候选生成)次日开盘未知, permissive开盘价(-20%)\n        # 落在带状窗口[-4,-2)之外会误杀全部候选 → 跳过窗口过滤只筛形态;\n        # 9:25 live模式(真实开盘价注入)与回测(无_mode属性)窗口照常生效\n        # (同 Task#36 B2-A 已验证模板)\n        if getattr(data_feed, '_mode', None) == 'signal':\n            win = np.ones(len(codes), dtype=bool)\n        not_bj = ~codes.str.startswith('bj.')\n        st = f_today['isST'].fillna(0).astype(int).to_numpy() \u003e 0\n        if 'code_name' in f_today.columns:\n            st = st | f_today['code_name'].fillna('').astype(str) \\\n                .str.upper().str.contains('ST').to_numpy()\n\n        mask = pattern \u0026 win \u0026 (opn \u003e 0) \u0026 not_bj \u0026 ~st\n        if not mask.any():\n            return []\n\n        # Task#50: D0封板时间过滤(首个 hourN_high \u003e= D0涨停价 的N)\n        if self.seal_filter in ('early', 'late'):\n            sub0 = f_d0.reindex(codes)\n            pre0 = sub0['preclose'].to_numpy(dtype=float, na_value=0.0)\n            # [Task#299] 同上, D0涨停价统一trading_rules口径\n            lp0 = np.array([trading_rules.limit_prices(c, p)[0]\n                            for c, p in zip(codes, pre0)])\n            seal_h = np.zeros(len(codes), dtype=np.int8)\n            for hh in (4, 3, 2, 1):          # 前面小时后写覆盖 → 首次触板\n                col = f'hour{hh}_high'\n                if col not in sub0.columns:\n                    continue\n                hi0 = sub0[col].to_numpy(dtype=float, na_value=0.0)\n                seal_h = np.where((hi0 \u003e 0) \u0026 (hi0 \u003e= lp0 - 0.001),\n                                  hh, seal_h)\n            if self.seal_filter == 'early':\n                mask \u0026= (seal_h \u003e= 1) \u0026 (seal_h \u003c= 2)\n            else:\n                mask \u0026= seal_h \u003e= 3\n            if not mask.any():\n                return []\n\n        # 排序: D0换手升序(低换手优先, 买入时刻可得)\n        d0_turn = f_d0.reindex(codes)['turn'] \\\n            .to_numpy(dtype=float, na_value=np.inf)\n        idx = np.where(mask)[0]\n        idx = idx[np.argsort(d0_turn[idx], kind='stable')]\n\n        final = []\n        for i in idx:\n            code = codes[i]\n            hist = data_feed.get_stock_history(code, date,\n                                               self.min_history_bars)\n            if len(hist) \u003c self.min_history_bars:\n                continue\n            final.append(code)\n\n        self._cand_codes = final\n        return final\n\n    def should_buy(self, code, date, hour, data_feed, portfolio):\n        if hour != self.buy_hour:\n            return None\n        if date != self._cand_date or code not in self._cand_codes:\n            return None\n        price = data_feed.get_hour_open(code, date, hour)\n        if not price or price \u003c= 0:\n            return None\n        limit_up, _ = data_feed.get_limit_prices(code, date)\n        if limit_up \u003e 0 and price \u003e= limit_up - 0.001:\n            return None\n        return Signal(code=code, price=float(price), strategy_name=self.name,\n                      target_hold_hours=self.max_hold_hours)\n\n    def should_sell(self, position, date, hour, data_feed):\n        # T+1: 买入当日不卖(框架另有兜底)\n        if position.buy_date \u003e= date:\n            return None\n        if self.exit_mode in self._H1_MODES:\n            return self._sell_forward(position, date, hour, data_feed)\n        buy_price = position.buy_price\n        if buy_price \u003c= 0:\n            return None\n\n        tp_target = buy_price * (1 + self.take_profit_pct)\n        sl_target = buy_price * (1 + self.stop_loss_pct)\n\n        bar_open = data_feed.get_hour_open(position.code, date, hour)\n        bar_high = data_feed.get_hour_high(position.code, date, hour)\n        bar_low = data_feed.get_hour_low(position.code, date, hour)\n        if not bar_open or bar_open \u003c= 0:\n            return None\n\n        # 跳空穿越按开盘成交(先判SL, 保守)\n        open_pnl = (bar_open - buy_price) / buy_price\n        if open_pnl \u003c= self.stop_loss_pct:\n            return SellSignal(reason='stop_loss', price=float(bar_open))\n        if open_pnl \u003e= self.take_profit_pct:\n            return SellSignal(reason='take_profit', price=float(bar_open))\n\n        # 盘中触发: 同小时TP/SL双触发保守取SL(小时内OHLC顺序未知)\n        sl_hit = bar_low and bar_low \u003e 0 and bar_low \u003c= sl_target\n        tp_hit = bar_high and bar_high \u003e 0 and bar_high \u003e= tp_target\n        if sl_hit:\n            return SellSignal(reason='stop_loss', price=float(sl_target))\n        if tp_hit:\n            return SellSignal(reason='take_profit', price=float(tp_target))\n\n        # 到期兜底: tpsl_d6=D6收盘(hours_held\u003e=20); tpsl_d2c=D2收盘(买入次日即到期)\n        expired = (position.hours_held \u003e= self.max_hold_hours\n                   if self.exit_mode == 'tpsl_d6' else True)\n        if expired and hour == 4:\n            price = data_feed.get_hour_close(position.code, date, 4)\n            if not price or price \u003c= 0:\n                price = bar_open\n            return SellSignal(reason='expired', price=float(price))\n        return None\n\n    def _sell_forward(self, position, date, hour, data_feed):\n        \"\"\"Task#12 前移变体: D2 H1内了结, 卖出当小时slot即可复用。\n        跌停封死/缺数据顺延 → 之后任意小时尽快开盘价卖出(deferred)。\"\"\"\n        code = position.code\n        if hour != 1:\n            price = data_feed.get_hour_open(code, date, hour)\n            if price and price \u003e 0:\n                return SellSignal(reason='deferred', price=float(price))\n            return None\n        o = data_feed.get_hour_open(code, date, 1)\n        c = data_feed.get_hour_close(code, date, 1)\n        lo = data_feed.get_hour_low(code, date, 1)\n        hi = data_feed.get_hour_high(code, date, 1)\n        if not o or o \u003c= 0:\n            return None\n\n        if self.exit_mode == 'd2h1_open':\n            return SellSignal(reason='expired', price=float(o))\n\n        if self.exit_mode == 'd2h1_close':\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n\n        if self.exit_mode == 'trail_h1':\n            # 保守挂单语义: 触发价只基于H1_open, 不做bar内peak乐观假设\n            trig = o * (1 - self.trail_pp / 100)\n            if lo and lo \u003e 0 and lo \u003c= trig:\n                return SellSignal(reason='stop_loss', price=float(trig))\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n\n        if self.exit_mode == 'tpsl_h1':\n            bp = position.buy_price\n            tp_p = bp * (1 + self.take_profit_pct)\n            sl_p = bp * (1 + self.stop_loss_pct)\n            if o \u003c= sl_p:\n                return SellSignal(reason='stop_loss', price=float(o))\n            if o \u003e= tp_p:\n                return SellSignal(reason='take_profit', price=float(o))\n            if lo and lo \u003e 0 and lo \u003c= sl_p:      # 双触发保守取SL\n                return SellSignal(reason='stop_loss', price=float(sl_p))\n            if hi and hi \u003e 0 and hi \u003e= tp_p:\n                return SellSignal(reason='take_profit', price=float(tp_p))\n            price = c if c and c \u003e 0 else o       # 未触发: H1收盘强平\n            return SellSignal(reason='expired', price=float(price))\n        return None\n\n    def get_buy_price(self, code, date, hour, data_feed):\n        return data_feed.get_hour_open(code, date, hour)\n","lineDetails":[{"lines":["1-31","33-66","71-75","80-124","129-143","150-159","182-306"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"},{"lines":["32","125-128","164-166"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"e66d27c3-d3c2-4f26-a172-7d07018434d9","userQueryBusinessId":"b18f83e8-8560-44f3-a478-c98127e22dce"},{"lines":["67-70","144-149"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"2e1ba1ac-3013-4fbb-8557-e44020720c88"},{"lines":["76-79","160-163","167-181"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"9e4613b1-8192-47c2-b50f-1e46ed9160e6"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/scripts/t11_seed2_monetize.py","aiAddedLines":["1-246"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"Task #11 种子2(恐慌H1确认反弹 PanicH1Confirm) 单月明细 + 收敛过滤 + 变现路径粗测\n\n种子2口径(Task #8): D0收\u003c=-2% \u0026 pos20\u003c-15 \u0026 D0盘后red_ratio\u003e=50\n                    \u0026 D1_H1涨幅(d1h1r=H1close/H1open-1)\u003e1.5% -\u003e D1 H2_open买, D2收卖\n已知难题: 池子级+0.70%净/6年全正, 但中性代码序top1仅+0.15% -\u003e 变现是成败关键。\n\n本脚本三件事:\n  P1 单月hour级明细(自动选: 恐慌月=信号最多月, 普通月=信号数中位月), 逐日+分段收益分解\n  P2 收敛过滤实验(池子级分年): H1阈值/深度/板块/流动性/red分层/防追高cap\n  P3 变现路径粗测: 确定性hash中性抽样 topK(1/3/5) x 卖点(D2收/D2H1收/D2H2o/D3收)\n     + slot年化粗估((1+r)^笔数-1, 周转按持有期算)\n\n数据截止2026-05-14(red_ratio缺口, Robin在修)。净=毛-0.2%。全程T+1合规。\n用法: python3 scripts/t11_seed2_monetize.py \u003e data/realtime/t11_seed2_monetize.txt 2\u003e\u00261\n\"\"\"\nimport sqlite3\nimport time\n\nimport numpy as np\nimport pandas as pd\n\nDB = '/home/AIWealth/data/stocks.db'\nEMO = '/home/AIWealth/data/realtime/emotion_cycle_daily.csv'\nT0 = time.time()\nCOST = 0.2\nEND_DATE = 20260514  # red_ratio数据缺口约束(2026-05-15后缺, Robin修复中)\n\n\ndef log(msg):\n    print(f\"[{time.time()-T0:7.1f}s] {msg}\", flush=True)\n\n\ndef load_data():\n    cols = (\"code, date, preclose, open, open_rate, close, close_rate, amount, isST, \"\n            \"hour1_open, hour1_close, hour2_open, hour2_close, hour3_close, hour4_close\")\n    conn = sqlite3.connect(f'file:{DB}?mode=ro', uri=True)\n    chunks = []\n    for ch in pd.read_sql(f\"SELECT {cols} FROM stock_kline WHERE date \u003e= '2020-10-01' \"\n                          \"ORDER BY code, date\", conn, chunksize=600_000):\n        for c in ch.columns:\n            if ch[c].dtype == np.float64:\n                ch[c] = ch[c].astype(np.float32)\n        ch['date'] = ch['date'].str.replace('-', '', regex=False).astype(np.int32)\n        ch['isST'] = ch['isST'].fillna(0).astype(np.int8)\n        chunks.append(ch)\n    conn.close()\n    df = pd.concat(chunks, ignore_index=True)\n    df['code'] = df['code'].astype('category')\n    log(f\"加载完成 rows={len(df)}\")\n    return df\n\n\ndef prepare(df):\n    code = df['code'].astype(str)\n    ratio = pd.Series(np.float32(0.10), index=df.index)\n    ratio[code.str.startswith('bj.')] = np.float32(0.30)\n    ratio[code.str.startswith('sz.30') | code.str.startswith('sh.688')] = np.float32(0.20)\n    df['board'] = np.where(code.str.startswith(('sz.30', 'sh.688')), 'GEM/STAR',\n                  np.where(code.str.startswith('bj.'), 'BJ', 'MAIN'))\n    df['limit_price'] = (df['preclose'] * (1 + ratio)).round(2)\n    df['limit_dn_price'] = (df['preclose'] * (1 - ratio)).round(2)\n    df['is_limit'] = (df['close'] \u003e= df['limit_price'] - 0.001) \u0026 (df['preclose'] \u003e 0)\n    df['is_dnlimit'] = (df['close'] \u003c= df['limit_dn_price'] + 0.001) \u0026 (df['preclose'] \u003e 0)\n\n    g = df.groupby('code', observed=True, sort=False)\n    df['days_listed'] = g.cumcount()\n    df['vol20'] = g['close_rate'].transform(lambda s: s.rolling(20, min_periods=10).std())\n    df['max20'] = g['close'].transform(lambda s: s.rolling(20, min_periods=10).max())\n    df['pos20'] = (df['close'] / df['max20'] - 1) * 100\n\n    for c in ('date', 'open', 'hour1_open', 'hour1_close', 'hour2_open', 'hour2_close',\n              'hour3_close', 'hour4_close', 'limit_price'):\n        df['d1_' + c] = g[c].shift(-1)\n    for c in ('open', 'hour1_close', 'hour2_open', 'hour4_close'):\n        df['d2_' + c] = g[c].shift(-2)\n    df['d3_hour4_close'] = g['hour4_close'].shift(-3)\n\n    df['d1h1r'] = (df['d1_hour1_close'] / df['d1_hour1_open'] - 1) * 100\n    df['year'] = (df['date'] // 10000).astype(str)\n    df['ym'] = df['date'] // 100\n\n    buy = df['d1_hour2_open']\n    # 卖点族(全部T+1合规: 买D1 H2o, 最早D2)\n    df['ret_d2c'] = (df['d2_hour4_close'] / buy - 1) * 100      # baseline\n    df['ret_d2h1c'] = (df['d2_hour1_close'] / buy - 1) * 100    # D2 H1收卖(避H3)\n    df['ret_d2h2o'] = (df['d2_hour2_open'] / buy - 1) * 100     # D2 H2开卖(整1天周期)\n    df['ret_d2o'] = (df['d2_open'] / buy - 1) * 100             # D2竞价卖(最快周转)\n    df['ret_d3c'] = (df['d3_hour4_close'] / buy - 1) * 100      # 多持1晚\n    # D1日内路径分解(持仓观察, 非卖点)\n    df['seg_h2'] = (df['d1_hour2_close'] / buy - 1) * 100\n    df['seg_h3'] = (df['d1_hour3_close'] / df['d1_hour2_close'] - 1) * 100\n    df['seg_h4'] = (df['d1_hour4_close'] / df['d1_hour3_close'] - 1) * 100\n    df['seg_ovn'] = (df['d2_open'] / df['d1_hour4_close'] - 1) * 100\n    df['seg_d2h1'] = (df['d2_hour1_close'] / df['d2_open'] - 1) * 100\n\n    # 确定性hash中性键(实盘可复现: 6位数字代码*大素数 xor 买入日) -\u003e 伪随机抽样\n    code_num = code.str[-6:].astype(np.int64)\n    df['hkey'] = ((code_num * 1000003) ^ df['d1_date'].fillna(0).astype(np.int64)) % 104729\n    return df\n\n\ndef block(sel, ret_col, label, by='year'):\n    print(f\"\\n### {label}\")\n    print(f\"{'期':\u003c9}{'笔数':\u003e7}{'胜率':\u003e8}{'净均值':\u003e9}{'中位':\u003e9}{'p10':\u003e9}{'信号日':\u003e7}\")\n    keys = (sorted(sel[by].unique()) if by == 'ym'\n            else ('2021', '2022', '2023', '2024', '2025', '2026'))\n    for y in list(keys) + ['ALL']:\n        gdf = sel if y == 'ALL' else sel[sel[by] == y]\n        v = gdf[ret_col].dropna()\n        if len(v) \u003c 5:\n            continue\n        nd = gdf.dropna(subset=[ret_col])['d1_date'].nunique()\n        print(f\"{str(y):\u003c9}{len(v):\u003e7}{(v\u003e0).mean()*100:\u003e7.1f}%{v.mean()-COST:\u003e+8.2f}%\"\n              f\"{v.median()-COST:\u003e+8.2f}%{v.quantile(0.1):\u003e+8.2f}%{nd:\u003e7}\")\n\n\ndef hash_topk(sub, k):\n    return (sub.sort_values(['d1_date', 