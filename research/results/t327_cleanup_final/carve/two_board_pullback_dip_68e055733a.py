7k(H1唯一系统性正时段) x Task#9归因(hold1占slot隔日才空)。\n    23→\n    24→卖出参数声明: hold1 策略无盘中止盈止损, 声明极宽 TP/SL 仅为满足框架读取要求,\n    25→实际退出 = 买入次日 hour4 收盘(expired); 跌停封死顺延后尽快卖出(deferred)。\n    26→\"\"\"\n    27→import sqlite3\n    28→\n    29→import numpy as np\n    30→\n    31→from strategies.base import Strategy, Signal, SellSignal\n    32→\n    33→_DB = '/home/AIWealth/data/stocks.db'\n    34→\n    35→\n    36→def _limit_flags(frame, codes):\n    37→    \"\"\"返回 (valid, is_limit, is_yizi, close) 对齐 codes 的numpy数组。\n    38→\n    39→    涨停判定与研究口径一致: close \u003e= round(preclose*(1+ratio),2) - 0.001\n    40→    一字板: open \u003e= 涨停价 - 0.001。ST股已在候选层排除, 此处用非ST比例。\n    41→    \"\"\"\n    42→    sub = frame.reindex(codes)\n    43→    pre = sub['preclose'].to_numpy(dtype=float, na_value=0.0)\n    44→    clo = sub['close'].to_numpy(dtype=float, na_value=0.0)\n    45→    opn = sub['open'].to_numpy(dtype=float, na_value=0.0)\n    46→    is20 = codes.str.startswith('sz.30') | codes.str.startswith('sh.688')\n    47→    ratio = np.where(is20, 0.20, 0.10)\n    48→    lp = np.round(pre * (1 + ratio), 2)\n    49→    valid = (pre \u003e 0) \u0026 (clo \u003e 0)\n    50→    is_lim = valid \u0026 (clo \u003e= lp - 0.001)\n    51→    is_yizi = is_lim \u0026 (opn \u003e 0) \u0026 (opn \u003e= lp - 0.001)\n    52→    return valid, is_lim, is_yizi, clo\n    53→\n    54→\n    55→def _lim_price(row, code):\n    56→    \"\"\"单行涨停价(与_limit_flags同口径); Task#68 F2过滤用。\"\"\"\n    57→    ratio = 0.20 if code.startswith(('sz.30', 'sh.688')) else 0.10\n    58→    pre = float(row.get('preclose') or 0)\n    59→    return round(pre * (1 + ratio), 2) if pre \u003e 0 else 0.0\n    60→\n    61→\n    62→def _is_lim(row, code):\n    63→    lp = _lim_price(row, code)\n    64→    c = float(row.get('close') or 0)\n    65→    return lp \u003e 0 and c \u003e 0 and c \u003e= lp - 0.001\n    66→\n    67→\n    68→def _load_red_map() -\u003e dict:\n    69→    \"\"\"{date: red_ratio(float)}; 缺失日不在map中, 调用方显式跳过。\"\"\"\n    70→    out = {}\n    71→    try:\n    72→        conn = sqlite3.connect(_DB)\n    73→        for d, r in conn.execute(\n    74→                \"SELECT date, red_ratio FROM index_kline \"\n    75→                \"WHERE code='sh.000001'\"):\n    76→            if r is not None:\n    77→                out[d] = float(r)\n    78→        conn.close()\n    79→    except sqlite3.Error:\n    80→        pass\n    81→    return out\n    82→\n    83→\n    84→class TwoBoardPullbackDipStrategy(Strategy):\n    85→    \"\"\"B2-A: 恰2板回调1~2天 → 低开-4~0 → H1低吸 → 次日收盘卖出。\"\"\"\n    86→\n    87→    name = \"two_board_pullback_dip\"\n    88→    max_hold_hours = 8           # D1 h1 买入 → D2 h4 收盘 = 8小时\n    89→    sell_day_no_buy = True\n    90→    buy_hour = 1\n    91→\n    92→    # === 核心参数(Task#7 网格最优) ===\n    93→    open_rate_min = -4.0         # 竞价低开下限\n    94→    open_rate_max = 0.0          # 竞价低开上限(不含)\n    95→    red_ratio_max = None         # B2-B变体设为40.0; None=不启用红盘过滤\n    96→    min_history_bars = 20        # 上市未满20根K线不买(对齐研究口径 nth\u003e=20)\n    97→    # Task#63/68 F2板质量过滤: 第2板首触涨停hour \u003e 该值则剔除(1=只留H1早板);\n    98→    # None=不过滤零侵入。逻辑自 scripts/t63_engine_runner.py T63Variant 转正,\n    99→    # 特征全部来自板日(D-1..D-4)K线, D1开盘前可见, 时序合规\n   100→    max_seal_hour_b2 = None\n   101→\n   102→    # hold1 无盘中TP/SL; 极宽声明仅为满足框架卖出参数读取要求(不会触发)\n   103→    take_profit_pct = 9.9