 D 变体: H1确认强度上限(避免追高) 1.5~4%\n    D_cap = D[D['d1h1r'] \u003c= 4]\n    pool_yearly(D_cap, 'ret_h2o_d2c', 'D+低位 \u0026 H1反弹1.5~4%(防追高)')\n    topk_yearly(D_cap, 'ret_h2o_d2c', 'amount', False, 3, 'D+低位 \u0026 H1反弹1.5~4%')\n    log(\"D组完成\")\n\n    print(\"\\n\" + \"=\" * 100)\n    print(\"E组: vol20\u003e5 \u0026 pos20\u003c-15 \u0026 D0\u003c=-5 \u0026 D1低开[-5,-1], D1H1o买 D5收卖 —— 2026规避测试\")\n    print(\"=\" * 100)\n    E = df[base \u0026 buyable_h1 \u0026 (df['vol20'] \u003e 5) \u0026 (df['pos20'] \u003c -15)\n           \u0026 (df['close_rate'] \u003c= -5)\n           \u0026 (df['d1_open_rate'] \u003e= -5) \u0026 (df['d1_open_rate'] \u003c= -1)].copy()\n    E_emo = E[E['red_ratio'] \u003e= 50]\n    pool_yearly(E_emo, 'ret_h1o_d5c', 'E+低开 + red_ratio\u003e=50')\n    E_emo2 = E[E['zha_rate'] \u003c= 0.35]\n    pool_yearly(E_emo2, 'ret_h1o_d5c', 'E+低开 + D0炸板率\u003c=35%(情绪不冰点)')\n    topk_yearly(E_emo, 'ret_h1o_d5c', 'amount', False, 3, 'E+低开 + red_ratio\u003e=50')\n    log(\"E组完成\")\n\n    print(\"\\n[口径] 净=毛-0.2%; 排序键/情绪指标均为D0盘后或D1买入时刻前可得; T+1合规。\")\n    log(\"全部完成\")\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-194"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/scripts/research_buysell_neutral_pick.py","aiAddedLines":["1-156"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"Task #8 第四步: 中性选择验证 —— 池子弥散alpha能否在slot=1/3下保留?\n\n第三步结论: 极端排序(vol/低开深度/H1强度/成交额)的topK全部跑输池子均值。\n假说: alpha弥散于池子, 选择偏差是唯一杀手 =\u003e 用与收益无关的确定性排序(代码字典序)\n     做topK, 应逼近池子均值。若成立, slot=1实盘即可用\"池内任取\"落地。\n\n对象(第三步幸存池):\n  P1 = D+低位+red50: D0收\u003c=-2 \u0026 pos20\u003c-15 \u0026 D1H1涨1.5%+ \u0026 red_ratio\u003e=50 -\u003e D1H2o买 D2收卖\n  P2 = B+低位+red50: D0收-9~-5 \u0026 D1低开[-5,-1] \u0026 pos20\u003c-15 \u0026 red_ratio\u003e=50 -\u003e D1H1o买 D5收卖\n  P3 = A+red60:      vol20\u003e5 \u0026 pos20\u003c-15 \u0026 red_ratio\u003e=60 -\u003e D1H1o买 D5收卖\n\n用法: python3 scripts/research_buysell_neutral_pick.py \u003e data/realtime/buysell_neutral_pick.txt 2\u003e\u00261\n\"\"\"\nimport sqlite3\nimport time\n\nimport numpy as np\nimport pandas as pd\n\nDB = '/home/AIWealth/data/stocks.db'\nEMO = '/home/AIWealth/data/realtime/emotion_cycle_daily.csv'\nT0 = time.time()\nCOST = 0.2\n\n\ndef log(msg):\n    print(f\"[{time.time()-T0:7.1f}s] {msg}\", flush=True)\n\n\ndef load_data():\n    cols = (\"code, date, preclose, open, high, close, close_rate, open_rate, \"\n            \"turn, amount, isST, hour1_open, hour1_close, hour2_open, hour4_close\")\n    conn = sqlite3.connect(f'file:{DB}?mode=ro', uri=True)\n    chunks = []\n    for ch in pd.read_sql(f\"SELECT {cols} FROM stock_kline WHERE date \u003e= '2020-10-01' \"\n                          \"ORDER BY code, date\", conn, chunksize=600_000):\n        for c in ch.columns:\n            if ch[c].dtype == np.float64:\n                ch[c] = ch[c].astype(np.float32)\n        ch['date'] = ch['date'].str.replace('-', '', regex=False).astype(np.int32)\n        ch['isST'] = ch['isST'].fillna(0).astype(np.int8)\n        chunks.append(ch)\n    conn.close()\n    df = pd.concat(chunks, ignore_index=True)\n    log(f\"加载完成 rows={len(df)}\")\n    return df\n\n\ndef prepare(df):\n    code = df['code']\n    ratio = pd.Series(np.float32(0.10), index=df.index)\n    ratio[code.str.startswith('bj.')] = np.float32(0.30)\n    ratio[code.str.startswith('sz.30') | code.str.startswith('sh.688')] = np.float32(0.20)\n    df['board'] = np.where(code.str.startswith(('sz.30', 'sh.688')), 'GEM/STAR',\n                  np.where(code.str.startswith('bj.'), 'BJ', 'MAIN'))\n    df['limit_price'] = (df['preclose'] * (1 + ratio)).round(2)\n    df['limit_dn_price'] = (df['preclose'] * (1 - ratio)).round(2)\n    df['is_dnlimit'] = (df['close'] \u003c= df['limit_dn_price'] + 0.001) \u0026 (df['preclose'] \u003e 0)\n\n    g = df.groupby('code', sort=False)\n    df['days_listed'] = g.cumcount()\n    df['vol20'] = g['close_rate'].transform(lambda s: s.rolling(20, min_periods=10).std())\n    df['max20'] = g['close'].transform(lambda s: s.rolling(20, min_periods=10).max())\n    df['pos20'] = (df['close'] / df['max20'] - 1) * 100\n\n    df['d1_date'] = g['date'].shift(-1)\n    df['d1_open_rate'] = g['open_rate'].shift(-1)\n    for c in ('hour1_open', 'hour1_close', 'hour2_open', 'limit_price'):\n        df['d1_' + c] = g[c].shift(-1)\n    df['d2_hour4_close'] = g['hour4_close'].shift(-2)\n    df['d5_hour4_close'] = g['hour4_close'].shift(-5)\n    df['d1h1r'] = (df['d1_hour1_close'] / df['d1_hour1_open'] - 1) * 100\n    df['year'] = (df['date'] // 10000).astype(str)\n    df['ret_h1o_d5c'] = (df['d5_hour4_close'] / df['d1_hour1_open'] - 1) * 100\n    df['ret_h2o_d2c'] = (df['d2_hour4_close'] / df['d1_hour2_open'] - 1) * 100\n    return df\n\n\ndef block(sel, ret_col, label):\n    print(f\"\\n### {label}\")\n    print(f\"{'年份':\u003c7}{'笔数':\u003e7}{'胜率':\u003e8}{'净均值':\u003e9}{'中位':\u003e9}{'p10':\u003e9}\")\n    for y in ('2021', '2022', '2023', '2024', '2025', '2026', 'ALL'):\n        gdf = sel if y == 'ALL' else sel[sel['year'] == y]\n        v = gdf[ret_col].dropna()\n        if len(v) \u003c 5:\n            continue\n        print(f\"{y:\u003c7}{len(v):\u003e7}{(v\u003e0).mean()*100:\u003e7.1f}%{v.mean()-COST:\u003e+8.2f}%\"\n              f\"{v.median()-COST:\u003e+8.2f}%{v.quantile(0.1):\u003e+8.2f}%\")\n\n\ndef neutral_variants(pool, ret_col, name):\n    \"\"\"中性/半中性选择: 代码序(与收益无关) topK; 以及每日等权(=池子日均, 分散化基准)。\"\"\"\n    pool = pool.dropna(subset=[ret_col])\n    for k in (1, 3):\n        sel = pool.sort_values(['d1_date', 'code']).groupby('d1_date').head(k)\n        block(sel, ret_col, f\"{name} 中性代码序 top{k}/日\")\n    # 每日等权组合(每信号日一笔, 收益=当日池子均值) =\u003e 分散化可达的上限\n    daily = pool.groupby(['d1_date'])[ret_col].agg(['mean', 'count'])\n    daily['year'] = (daily.index // 10000).astype(str)\n    print(f\"\\n### {name} 每日等权(全池分散, 理论口径)\")\n    print(f\"{'年份':\u003c7}{'交易日':\u003e7}{'日胜率':\u003e8}{'日净均值':\u003e10}{'平均持仓数':\u003e10}\")\n    for y in ('2021', '2022', '2023', '2024', '2025', '2026', 'ALL'):\n        gdf = daily if y == 'ALL' else daily[daily['year'] == y]\n        if len(gdf) \u003c 3:\n            continue\n        print(f\"{y:\u003c7}{len(gdf):\u003e7}{(gdf['mean']\u003e0).mean()*100:\u003e7.1f}%\"\n              f\"{gdf['mean'].mean()-COST:\u003e+9.2f}%{gdf['count'].mean():\u003e10.1f}\")\n\n\ndef main():\n    df = load_data()\n    df = prepare(df)\n    emo = pd.read_csv(EMO)\n    emo['date'] = emo['date'].str.replace('-', '', regex=False).astype(np.int32)\n    emo = emo.set_index('date')\n    df['red_ratio'] = df['date'].map(emo['red_ratio'])\n    log(\"预处理完成\")\n\n    base = ((df['date'] \u003e= 20210101) \u0026 (df['isST'] == 0) \u0026 (df['board'] != 'BJ')\n            \u0026 (df['days_listed'] \u003e= 60) \u0026 (df['close'] \u003e 2) \u0026 (df['preclose'] \u003e 0)\n            \u0026 (df['hour1_open'] \u003e 0) \u0026 (df['hour4_close'] \u003e 0) \u0026 ~df['is_dnlimit'])\n    buyable_h1 = (df['d1_hour1_open'] \u003e 0) \u0026 (df['d1_hour1_open'] \u003c df['d1_limit_price'] - 0.001)\n    buyable_h2 = (df['d1_hour2_open'] \u003e 0) \u0026 (df['d1_hour2_open'] \u003c df['d1_limit_price'] - 0.001)\n\n    print(\"=\" * 100)\n    print(\"P1: 恐慌H1确认反弹+低位+red50  D1H2o买 D2收卖 (第三步池子级6年全正)\")\n    print(\"=\" * 100)\n    P1 = df[base \u0026 buyable_h2 \u0026 (df['close_rate'] \u003c= -2) \u0026 (df['pos20'] \u003c -15)\n            \u0026 (df['d1h1r'] \u003e 1.5) \u0026 (df['red_ratio'] \u003e= 50)].copy()\n    neutral_variants(P1, 'ret_h2o_d2c', 'P1')\n    log(\"P1完成\")\n\n    print(\"\\n\" + \"=\" * 100)\n    print(\"P2: 大跌修复+低位+red50  D1H1o买 D5收卖\")\n    print(\"=\" * 100)\n    P2 = df[base \u0026 buyable_h1 \u0026 (df['close_rate'] \u003c= -5) \u0026 (df['close_rate'] \u003e -9)\n            \u0026 (df['d1_open_rate'] \u003e= -5) \u0026 (df['d1_open_rate'] \u003c= -1)\n            \u0026 (df['pos20'] \u003c -15) \u0026 (df['red_ratio'] \u003e= 50)].copy()\n    neutral_variants(P2, 'ret_h1o_d5c', 'P2')\n    log(\"P2完成\")\n\n    print(\"\\n\" + \"=\" * 100)\n    print(\"P3: 高波动低位+red60  D1H1o买 D5收卖\")\n    print(\"=\" * 100)\n    P3 = df[base \u0026 buyable_h1 \u0026 (df['vol20'] \u003e 5) \u0026 (df['pos20'] \u003c -15)\n            \u0026 (df['red_ratio'] \u003e= 60)].copy()\n    neutral_variants(P3, 'ret_h1o_d5c', 'P3')\n    log(\"P3完成\")\n\n    print(\"\\n[口径] 净=毛-0.2%; 代码序=确定性且与收益无关的选择规则(实盘可执行); T+1合规。\")\n    log(\"全部完成\")\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-156"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/scripts/research_buysell_breadth_check.py","aiAddedLines":["1-136"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"Task #8 第五步: 广度择时验证 + 种子C月度归因\n\n1) P3(vol20\u003e5 \u0026 pos20\u003c-15 \u0026 red60)条件全为D0收盘可判 =\u003e 当日信号总数(广度)在D0盘后可得。\n   验证: 仅在广度\u003e=N日开仓, 能否修复2021/2026?\n2) 种子C(强者高开确认)ALL+2.54%净, 但2024独占+7.86% =\u003e 月度归因确认是否2024.09幻觉。\n\n用法: python3 scripts/research_buysell_breadth_check.py \u003e data/realtime/buysell_breadth_check.txt 2\u003e\u00261\n\"\"\"\nimport sqlite3\nimport time\n\nimport numpy as np\nimport pandas as pd\n\nDB = '/home/AIWealth/data/stocks.db'\nEMO = '/home/AIWealth/data/realtime/emotion_cycle_daily.csv'\nT0 = time.time()\nCOST = 0.2\n\n\ndef log(msg):\n    print(f\"[{time.time()-T0:7.1f}s] {msg}\", flush=True)\n\n\ndef load_data():\n    cols = (\"code, date, preclose, open, high, close, close_rate, open_rate, \"\n            \"amount, isST, hour1_open, hour2_open, hour4_close\")\n    conn = sqlite3.connect(f'file:{DB}?mode=ro', uri=True)\n    chunks = []\n    for ch in pd.read_sql(f\"SELECT {cols} FROM stock_kline WHERE date \u003e= '2020-10-01' \"\n                          \"ORDER BY code, date\", conn, chunksize=600_000):\n        for c in ch.columns:\n            if ch[c].dtype == np.float64:\n                ch[c] = ch[c].astype(np.float32)\n        ch['date'] = ch['date'].str.replace('-', '', regex=False).astype(np.int32)\n        ch['isST'] = ch['isST'].fillna(0).astype(np.int8)\n        chunks.append(ch)\n    conn.close()\n    df = pd.concat(chunks, ignore_index=True)\n    log(f\"加载完成 rows={len(df)}\")\n    return df\n\n\ndef prepare(df):\n    code = df['code']\n    ratio = pd.Series(np.float32(0.10), index=df.index)\n    ratio[code.str.startswith('bj.')] = np.float32(0.30)\n    ratio[code.str.startswith('sz.30') | code.str.startswith('sh.688')] = np.float32(0.20)\n    df['board'] = np.where(code.str.startswith(('sz.30', 'sh.688')), 'GEM/STAR',\n                  np.where(code.str.startswith('bj.'), 'BJ', 'MAIN'))\n    df['limit_price'] = (df['preclose'] * (1 + ratio)).round(2)\n    df['limit_dn_price'] = (df['preclose'] * (1 - ratio)).round(2)\n    df['is_limit'] = (df['close'] \u003e= df['limit_price'] - 0.001) \u0026 (df['preclose'] \u003e 0)\n    df['is_zha'] = (df['high'] \u003e= df['limit_price'] - 0.001) \u0026 ~df['is_limit'] \u0026 (df['preclose'] \u003e 0)\n    df['is_dnlimit'] = (df['close'] \u003c= df['limit_dn_price'] + 0.001) \u0026 (df['preclose'] \u003e 0)\n\n    g = df.groupby('code', sort=False)\n    df['days_listed'] = g.cumcount()\n    df['vol20'] = g['close_rate'].transform(lambda s: s.rolling(20, min_periods=10).std())\n    df['max20'] = g['close'].transform(lambda s: s.rolling(20, min_periods=10).max())\n    df['pos20'] = (df['close'] / df['max20'] - 1) * 100\n\n    df['d1_date'] = g['date'].shift(-1)\n    df['d1_open_rate'] = g['open_rate'].shift(-1)\n    df['d1_hour1_open'] = g['hour1_open'].shift(-1)\n    df['d1_limit_price'] = g['limit_price'].shift(-1)\n    df['d2_hour4_close'] = g['hour4_close'].shift(-2)\n    df['d5_hour4_close'] = g['hour4_close'].shift(-5)\n    df['year'] = (df['date'] // 10000).astype(str)\n    df['ym'] = (df['date'] // 100).astype(str)\n    df['ret_h1o_d2c'] = (df['d2_hour4_close'] / df['d1_hour1_open'] - 1) * 100\n    df['ret_h1o_d5c'] = (df['d5_hour4_close'] / df['d1_hour1_open'] - 1) * 100\n    return df\n\n\ndef block(sel, ret_col, label, by='year'):\n    print(f\"\\n### {label}\")\n    print(f\"{'期':\u003c9}{'笔数':\u003e7}{'胜率':\u003e8}{'净均值':\u003e9}{'中位':\u003e9}{'p10':\u003e9}\")\n    keys = sorted(sel[by].unique()) if by == 'ym' else ('2021', '2022', '2023', '2024', '2025', '2026')\n    for y in list(keys) + ['ALL']:\n        gdf = sel if y == 'ALL' else sel[sel[by] == y]\n        v = gdf[ret_col].dropna()\n        if len(v) \u003c 5:\n            continue\n        print(f\"{str(y):\u003c9}{len(v):\u003e7}{(v\u003e0).mean()*100:\u003e7.1f}%{v.mean()-COST:\u003e+8.2f}%\"\n              f\"{v.median()-COST:\u003e+8.2f}%{v.quantile(0.1):\u003e+8.2f}%\")\n\n\ndef main():\n    df = load_data()\n    df = prepare(df)\n    emo = pd.read_csv(EMO)\n    emo['date'] = emo['date'].str.replace('-', '', regex=False).astype(np.int32)\n    df['red_ratio'] = df['date'].map(emo.set_index('date')['red_ratio'])\n    log(\"预处理完成\")\n\n    base = ((df['date'] \u003e= 20210101) \u0026 (df['isST'] == 0) \u0026 (df['board'] != 'BJ')\n            \u0026 (df['days_listed'] \u003e= 60) \u0026 (df['close'] \u003e 2) \u0026 (df['preclose'] \u003e 0)\n            \u0026 (df['hour1_open'] \u003e 0) \u0026 (df['hour4_close'] \u003e 0) \u0026 ~df['is_dnlimit'])\n    buyable_h1 = (df['d1_hour1_open'] \u003e 0) \u0026 (df['d1_hour1_open'] \u003c df['d1_limit_price'] - 0.001)\n\n    # ---- P3 + 广度择时 ----\n    print(\"=\" * 100)\n    print(\"P3: vol20\u003e5 \u0026 pos20\u003c-15 \u0026 red_ratio\u003e=60 (D0收盘全可判) -\u003e 广度=当日满足条件股数\")\n    print(\"=\" * 100)\n    cond_p3 = base \u0026 (df['vol20'] \u003e 5) \u0026 (df['pos20'] \u003c -15) \u0026 (df['red_ratio'] \u003e= 60)\n    P3 = df[cond_p3 \u0026 buyable_h1].copy()\n    breadth = df[cond_p3].groupby('date').size()  # D0盘后可得(不含D1可执行性)\n    P3['breadth'] = P3['date'].map(breadth)\n    print(f\"广度分布: p25={breadth.quantile(0.25):.0f} p50={breadth.median():.0f} \"\n          f\"p75={breadth.quantile(0.75):.0f} p90={breadth.quantile(0.9):.0f}\")\n    for thr in (100, 200, 400):\n        sub = P3[P3['breadth'] \u003e= thr]\n        sel1 = sub.dropna(subset=['ret_h1o_d5c']).sort_values(['d1_date', 'code']).groupby('d1_date').head(1)\n        block(sel1, 'ret_h1o_d5c', f'P3 广度\u003e={thr} 中性top1/日 D5收卖')\n        sel3 = sub.dropna(subset=['ret_h1o_d5c']).sort_values(['d1_date', 'code']).groupby('d1_date').head(3)\n        block(sel3, 'ret_h1o_d5c', f'P3 广度\u003e={thr} 中性top3/日 D5收卖')\n    log(\"P3广度完成\")\n\n    # ---- 种子C 月度归因 ----\n    print(\"\\n\" + \"=\" * 100)\n    print(\"种子C: D0收\u003e=2%非涨停非炸板 \u0026 D1高开[3,5] -\u003e D1H1o买 D2收卖 —— 2024月度归因\")\n    print(\"=\" * 100)\n    C = df[base \u0026 buyable_h1 \u0026 (df['close_rate'] \u003e= 2) \u0026 ~df['is_limit'] \u0026 ~df['is_zha']\n           \u0026 (df['d1_open_rate'] \u003e= 3) \u0026 (df['d1_open_rate'] \u003c= 5)].copy()\n    C24 = C[C['year'] == '2024']\n    block(C24, 'ret_h1o_d2c', '种子C 2024年逐月', by='ym')\n    log(\"种子C月度完成\")\n\n    print(\"\\n[口径] 净=毛-0.2%; 广度在D0盘后可得(合规择时字段); T+1合规。\")\n    log(\"全部完成\")\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-136"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/scripts/task8_append_status.py","aiAddedLines":["1-53"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"Task #8 专属: 原子追加买卖点结构研究结论到 PROJECT_STATUS.md (插到AUTO分区之前)。一次性使用。\"\"\"\nimport os\n\nSTATUS = '/home/AIWealth/PROJECT_STATUS.md'\nMARK = '\u003c!-- AUTO_PROGRESS_MONITOR_START'\n\nSECTION = \"\"\"\n## 🔬 策略研究 Task #8: 自底向上买卖点结构挖掘 (2026-07-24)\n\n**方法**: 不预设形态, 全市场602万股票日 → 时段分解/入场×退出矩阵/缺口分布/日内结构 四维扫描 → 种子分年验证 + slot压力测试 + 情绪/广度择时。全程可执行口径(D0收盘+D1竞价/已完成小时决策, T+1, 剔除开盘涨停, 净=毛-0.2%)。\n\n### 结构性发现(6年稳定)\n1. **H1是A股唯一系统性正收益时段**, 强度随恐慌程度单调递增; **H3系统性最差**(全池胜率39.3%); 隔夜对买家为负(唯涨停股隔夜+1.72%后日内全负 → 涨停持仓开盘即卖是结构最优)\n2. 恐慌修复alpha的最优买点=D1 H1_open(每晚1小时期望-0.2~0.4pp), 低吸接入深度红线=低开-5%(≤-7%破位全形态负)\n3. **弥散alpha陷阱(重要方法论)**: 恐慌修复池子级为正, 但任何极值排序top1全负、中性代码序topK仅保留30-50% → 不是alpha假, 是\"选极端个体\"毁掉alpha。实盘化只有两条路: 条件收紧稀疏化事件 / 多slot等权分散\n4. 追强再证无alpha(涨停/炸板次日全买点×全卖点矩阵皆负); H1量比是排除项不是入选项(低开+H1放量\u003e2x更差)\n\n### 闪耀策略种子(建议rule2优先级)\n| 种子 | 触发→买卖点 | 全期净/笔 | 胜率 | n | 分年 |\n|---|---|---|---|---|---|\n| ★★★★★ 系统性恐慌修复 | [vol20\u003e5\u0026pos20\u003c-15\u0026red60]广度≥200 → D1H1o买3只(中性选择) D5收卖 | +2.52% | 64.2% | 201 | 2021:-0.1 22:+3.4 24:+3.8 25:+0.6 26:-1.0 |\n| ★★★★ 恐慌H1确认反弹 | D0≤-2%\u0026pos20\u003c-15\u0026red50 \u0026 D1H1涨\u003e1.5% → D1H2o买 D2收卖 | +0.70% | 55.4% | 10631 | **6年全正**(池子级) |\n| ★★★ 大跌低位修复 | D0收-9~-5%\u0026pos20\u003c-15\u0026red50\u0026D1低开-5~-1 → D1H1o买 D5收卖 | +3.01% | 58.6% | 2559 | 仅2026负(-1.7) |\n| ❌ 强者高开确认 | D0涨2%+\u0026D1高开3~5 | +2.54%表面 | — | 15489 | **2024-09单月幻觉**(该月98.5%胜率, 其余11月负), 否决存档 |\n\n### 给现有策略的免费线索\n涨停系卖点勿拖过H1(H3最差); 低吸买点勿等\"企稳确认\"; 深低开≤-7%不接; H1量比\u003e2x作排除条件。\n\n**资产**: scripts/research_buysell_{structure_scan,seed_validate,seed_refine,neutral_pick,breadth_check}.py; data/realtime/buysell_*.txt; 报告=data/realtime/RESEARCH_BUYSELL_STRUCTURE_REPORT.md\n\n---\n\n\"\"\"\n\n\ndef main():\n    with open(STATUS, 'r', encoding='utf-8') as f:\n        content = f.read()\n    if 'Task #8: 自底向上买卖点结构挖掘' in content:\n        print('已存在, 跳过')\n        return\n    idx = content.find(MARK)\n    new = content + SECTION if idx \u003c 0 else content[:idx] + SECTION + content[idx:]\n    tmp = STATUS + '.task8.tmp'\n    with open(tmp, 'w', encoding='utf-8') as f:\n        f.write(new)\n    os.replace(tmp, STATUS)\n    print('追加完成')\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-53"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/strategies/two_board_pullback_dip.py","aiAddedLines":["1-305"],"aiDeletedLines":[],"aiModifiedContent":"\"\"\"2板回调低吸策略 (B2-A) - Task#9 引擎精测。\n\n形态(来自 Task#7 全维度网格复审, 研究口径见 data/realtime/task7_refine.txt):\n  恰2板(最近涨停日连板数==2, 第2板非一字) → 断板回调1~2天(期间不再涨停)\n  → 买入日竞价低开 -4% \u003c= open_rate \u003c 0 → H1_open 买入 → hold1 次日收盘卖出\n排序: 更低开优先(open_rate升序) — task7_top1_probe: top1 +0.91%/日, 2021-2026六年全正\n\n研究口径(池子级 hold1, 毛收益): n=5388 胜率48.4% 均值+0.67% 六年全正\n引擎精测(2021-01~2026-07-23, slot=1, 含成本): CAGR +56.21% | MDD 73.10%\n  | 641笔 胜率49.45% +0.65%/笔 | 分年 21:+70 22:-33 23:+3 24:+46 25:+194 26:+121\n  → 观察池(50-100%档); 详见 data/realtime/task9_engine_report.txt\nTask#12 干净数据重跑(stocks.db 2026-05-25~06-30缺口修复后): CAGR +61.36%,\n  2026 +121→+174, 其余年份不变; 卖点前移变体 two_board_pullback_dip_h1c\n  (D2 H1收盘卖+slot当小时复用) **CAGR +107.79% 达标** → 推荐替代本对照版。\n变体 B2-B(two_board_pullback_dip_b): 加\"昨日红盘占比 red_ratio \u003c 40\"过滤,\n  red_ratio 取 D0(昨日)值(D0收盘后即确定, 无未来数据); 缺失日显式跳过不买入。\nred_ratio 权威源=index_kline(sh.000001), 已补齐到 2026-07-23(Task#10 Robin修复)。\n\nTask#12 卖点前移(exit_mode 参数化, 买入逻辑不动):\n  hold1_close 现行对照: D2 hour4 收盘卖\n  d2h1_open / d2h1_close / trail_h1: D2 H1内了结 → slot当小时可复用\n依据: Lee时段结构(H1唯一系统性正时段) x Task#9归因(hold1占slot隔日才空)。\n\n卖出参数声明: hold1 策略无盘中止盈止损, 声明极宽 TP/SL 仅为满足框架读取要求,\n实际退出 = 买入次日 hour4 收盘(expired); 跌停封死顺延后尽快卖出(deferred)。\n\"\"\"\nimport sqlite3\n\nimport numpy as np\n\nfrom strategies.base import Strategy, Signal, SellSignal\n\n_DB = '/home/AIWealth/data/stocks.db'\n\n\ndef _limit_flags(frame, codes):\n    \"\"\"返回 (valid, is_limit, is_yizi, close) 对齐 codes 的numpy数组。\n\n    涨停判定与研究口径一致: close \u003e= round(preclose*(1+ratio),2) - 0.001\n    一字板: open \u003e= 涨停价 - 0.001。ST股已在候选层排除, 此处用非ST比例。\n    \"\"\"\n    sub = frame.reindex(codes)\n    pre = sub['preclose'].to_numpy(dtype=float, na_value=0.0)\n    clo = sub['close'].to_numpy(dtype=float, na_value=0.0)\n    opn = sub['open'].to_numpy(dtype=float, na_value=0.0)\n    is20 = codes.str.startswith('sz.30') | codes.str.startswith('sh.688')\n    ratio = np.where(is20, 0.20, 0.10)\n    lp = np.round(pre * (1 + ratio), 2)\n    valid = (pre \u003e 0) \u0026 (clo \u003e 0)\n    is_lim = valid \u0026 (clo \u003e= lp - 0.001)\n    is_yizi = is_lim \u0026 (opn \u003e 0) \u0026 (opn \u003e= lp - 0.001)\n    return valid, is_lim, is_yizi, clo\n\n\ndef _lim_price(row, code):\n    \"\"\"单行涨停价(与_limit_flags同口径); Task#68 F2过滤用。\"\"\"\n    ratio = 0.20 if code.startswith(('sz.30', 'sh.688')) else 0.10\n    pre = float(row.get('preclose') or 0)\n    return round(pre * (1 + ratio), 2) if pre \u003e 0 else 0.0\n\n\ndef _is_lim(row, code):\n    lp = _lim_price(row, code)\n    c = float(row.get('close') or 0)\n    return lp \u003e 0 and c \u003e 0 and c \u003e= lp - 0.001\n\n\ndef _load_red_map() -\u003e dict:\n    \"\"\"{date: red_ratio(float)}; 缺失日不在map中, 调用方显式跳过。\"\"\"\n    out = {}\n    try:\n        conn = sqlite3.connect(_DB)\n        for d, r in conn.execute(\n                \"SELECT date, red_ratio FROM index_kline \"\n                \"WHERE code='sh.000001'\"):\n            if r is not None:\n                out[d] = float(r)\n        conn.close()\n    except sqlite3.Error:\n        pass\n    return out\n\n\nclass TwoBoardPullbackDipStrategy(Strategy):\n    \"\"\"B2-A: 恰2板回调1~2天 → 低开-4~0 → H1低吸 → 次日收盘卖出。\"\"\"\n\n    name = \"two_board_pullback_dip\"\n    max_hold_hours = 8           # D1 h1 买入 → D2 h4 收盘 = 8小时\n    sell_day_no_buy = True\n    buy_hour = 1\n\n    # === 核心参数(Task#7 网格最优) ===\n    open_rate_min = -4.0         # 竞价低开下限\n    open_rate_max = 0.0          # 竞价低开上限(不含)\n    red_ratio_max = None         # B2-B变体设为40.0; None=不启用红盘过滤\n    min_history_bars = 20        # 上市未满20根K线不买(对齐研究口径 nth\u003e=20)\n    # Task#63/68 F2板质量过滤: 第2板首触涨停hour \u003e 该值则剔除(1=只留H1早板);\n    # None=不过滤零侵入。逻辑自 scripts/t63_engine_runner.py T63Variant 转正,\n    # 特征全部来自板日(D-1..D-4)K线, D1开盘前可见, 时序合规\n    max_seal_hour_b2 = None\n\n    # hold1 无盘中TP/SL; 极宽声明仅为满足框架卖出参数读取要求(不会触发)\n    take_profit_pct = 9.99       # +999%, 实际不可达\n    stop_loss_pct = -0.99        # -99%, 实际不可达\n\n    # === Task#12 卖点前移 ===\n    # hold1_close(现行对照) | d2h1_open | d2h1_close | trail_h1\n    exit_mode = 'hold1_close'\n    trail_pp = 2.0               # trail_h1 回撤触发(pp), 基于D2 H1_open\n\n    def __init__(self):\n        self._cand_codes = []\n        self._cand_date = None\n        self._red_map = None\n\n    def _red_ratio(self, date: str):\n        if self._red_map is None:\n            self._red_map = _load_red_map()\n        return self._red_map.get(date)\n\n    def get_candidates(self, date, data_feed):\n        self._cand_codes, self._cand_date = [], date\n\n        # 回看5个交易日: d[0]=D0(昨日) .. d[4]\n        chain = []\n        d = date\n        for _ in range(5):\n            d = data_feed._prev_trading_date(d)\n            if not d:\n                return []\n            chain.append(d)\n\n        # B2-B: 昨日红盘占比过滤(D0收盘后已知); 缺失日显式跳过, 不引入未来数据\n        if self.red_ratio_max is not None:\n            red = self._red_ratio(chain[0])\n            if red is None or red \u003e= self.red_ratio_max:\n                return []\n\n        f_today = data_feed._load_day(date)\n        if f_today is None or f_today.empty:\n            return []\n        frames = [data_feed._load_day(d) for d in chain]\n        if any(f is None or f.empty for f in frames):\n            return []\n\n        codes = f_today.index\n        flags = [_limit_flags(f, codes) for f in frames]\n        v = [x[0] for x in flags]\n        L = [x[1] for x in flags]\n        Y = [x[2] for x in flags]\n\n        # 恰2板 + 回调1~2天(期间无涨停), 第2板非一字:\n        #   case1: D0未板, D-1/D-2连板, D-3未板   (断板后第1天)\n        #   case2: D0/D-1未板, D-2/D-3连板, D-4未板 (断板后第2天)\n        case1 = v[0] \u0026 v[1] \u0026 v[2] \u0026 v[3] \u0026 ~L[0] \u0026 L[1] \u0026 L[2] \u0026 ~L[3] \u0026 ~Y[1]\n        case2 = (v[0] \u0026 v[1] \u0026 v[2] \u0026 v[3] \u0026 v[4]\n                 \u0026 ~L[0] \u0026 ~L[1] \u0026 L[2] \u0026 L[3] \u0026 ~L[4] \u0026 ~Y[2])\n        pattern = case1 | case2\n\n        # 今日竞价窗口 + 基础池过滤\n        orate = f_today['open_rate'].to_numpy(dtype=float, na_value=np.nan)\n        opn = f_today['open'].to_numpy(dtype=float, na_value=0.0)\n        win = (orate \u003e= self.open_rate_min) \u0026 (orate \u003c self.open_rate_max)\n        # Task#36: signal模式(晚间候选生成)次日开盘未知, permissive开盘价(-20%)\n        # 落在带状窗口[-4,0)之外会误杀全部候选 → 跳过窗口过滤只筛形态;\n        # 9:25 live模式(真实开盘价注入)与回测(无_mode属性)窗口照常生效\n        if getattr(data_feed, '_mode', None) == 'signal':\n            win = np.ones(len(codes), dtype=bool)\n        not_bj = ~codes.str.startswith('bj.')\n        st = f_today['isST'].fillna(0).astype(int).to_numpy() \u003e 0\n        if 'code_name' in f_today.columns:\n            st = st | f_today['code_name'].fillna('').astype(str) \\\n                .str.upper().str.contains('ST').to_numpy()\n\n        mask = pattern \u0026 win \u0026 (opn \u003e 0) \u0026 not_bj \u0026 ~st\n        if not mask.any():\n            return []\n\n        # 排序: 更低开优先(open_rate升序)\n        idx = np.where(mask)[0]\n        idx = idx[np.argsort(orate[idx], kind='stable')]\n\n        final = []\n        for i in idx:\n            code = codes[i]\n            # 上市历史检查(对齐研究口径 nth\u003e=20), 仅对少量入围者查库\n            hist = data_feed.get_stock_history(code, date,\n                                               self.min_history_bars)\n            if len(hist) \u003c self.min_history_bars:\n                continue\n            final.append(code)\n\n        # Task#63/68 F2: 板质量过滤(第2板必须在max_seal_hour_b2内首触涨停);\n        # 与T63Variant完全同序同口径: 在基础候选链(含历史检查)之后叠加\n        if self.max_seal_hour_b2 is not None:\n            kept = []\n            for code in final:\n                br = self._board_rows(code, date, data_feed)\n                if br is None:\n                    continue                  # 板日重推失败: 保守剔除\n                _b1, b2 = br\n                if self._seal_hour(b2, code) \u003e self.max_seal_hour_b2:\n                    continue\n                kept.append(code)\n            final = kept\n\n        self._cand_codes = final\n        return final\n\n    def _board_rows(self, code, date, data_feed):\n        \"\"\"返回 (b1_row, b2_row) 或 None; 板日从回看链重推(与形态判定一致)。\"\"\"\n        chain = []\n        d = date\n        for _ in range(4):\n            d = data_feed._prev_trading_date(d)\n            if not d:\n                return None\n            chain.append(d)\n        frames = [data_feed._load_day(x) for x in chain]\n        if any(f is None or f.empty or code not in f.index for f in frames):\n            return None\n        # case1: 板=chain[1],chain[2]; case2: 板=chain[2],chain[3]\n        if _is_lim(frames[1].loc[code], code):\n            b2, b1 = frames[1].loc[code], frames[2].loc[code]\n        elif _is_lim(frames[2].loc[code], code):\n            b2, b1 = frames[2].loc[code], frames[3].loc[code]\n        else:\n            return None\n        return b1, b2\n\n    def _seal_hour(self, row, code):\n        \"\"\"首次触涨停时段(hourN_high\u003e=涨停价的最早N); 无hour数据返回0。\"\"\"\n        lp = _lim_price(row, code)\n        for h in (1, 2, 3, 4):\n            v = row.get(f'hour{h}_high')\n            if v is not None and np.isfinite(v) and v \u003e= lp - 0.001:\n                return h\n        return 0\n\n    def should_buy(self, code, date, hour, data_feed, portfolio):\n        if hour != self.buy_hour:\n            return None\n        if date != self._cand_date or code not in self._cand_codes:\n            return None\n        price = data_feed.get_hour_open(code, date, hour)\n        if not price or price \u003c= 0:\n            return None\n        # 双保险: 开盘已涨停不可买(引擎另有兜底)\n        limit_up, _ = data_feed.get_limit_prices(code, date)\n        if limit_up \u003e 0 and price \u003e= limit_up - 0.001:\n            return None\n        return Signal(code=code, price=float(price), strategy_name=self.name,\n                      target_hold_hours=self.max_hold_hours)\n\n    def should_sell(self, position, date, hour, data_feed):\n        # T+1: 买入当日不卖(框架另有兜底)\n        if position.buy_date \u003e= date:\n            return None\n        if self.exit_mode != 'hold1_close':\n            return self._sell_forward(position, date, hour, data_feed)\n        # hold1: 次日(D2) hour4 收盘卖出\n        if hour == 4:\n            price = data_feed.get_hour_close(position.code, date, 4)\n            if not price or price \u003c= 0:\n                price = data_feed.get_hour_open(position.code, date, 4)\n            if price and price \u003e 0:\n                return SellSignal(reason='expired', price=float(price))\n            return None\n        # 跌停封死顺延/停牌后的兜底: 已超过hold1目标仍持仓 → 尽快卖出\n        if position.hours_held \u003e= self.max_hold_hours:\n            price = data_feed.get_hour_open(position.code, date, hour)\n            if price and price \u003e 0:\n                return SellSignal(reason='deferred', price=float(price))\n        return None\n\n    def _sell_forward(self, position, date, hour, data_feed):\n        \"\"\"Task#12 前移变体: D2 H1内了结, 卖出当小时slot即可复用。\n        跌停封死/缺数据顺延 → 之后任意小时尽快开盘价卖出(deferred)。\"\"\"\n        code = position.code\n        if hour != 1:\n            price = data_feed.get_hour_open(code, date, hour)\n            if price and price \u003e 0:\n                return SellSignal(reason='deferred', price=float(price))\n            return None\n        o = data_feed.get_hour_open(code, date, 1)\n        c = data_feed.get_hour_close(code, date, 1)\n        lo = data_feed.get_hour_low(code, date, 1)\n        if not o or o \u003c= 0:\n            return None\n        if self.exit_mode == 'd2h1_open':\n            return SellSignal(reason='expired', price=float(o))\n        if self.exit_mode == 'd2h1_close':\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n        if self.exit_mode == 'trail_h1':\n            # 保守挂单语义: 触发价只基于H1_open, 不做bar内peak乐观假设\n            trig = o * (1 - self.trail_pp / 100)\n            if lo and lo \u003e 0 and lo \u003c= trig:\n                return SellSignal(reason='stop_loss', price=float(trig))\n            price = c if c and c \u003e 0 else o\n            return SellSignal(reason='expired', price=float(price))\n        return None\n\n    def get_buy_price(self, code, date, hour, data_feed):\n        return data_feed.get_hour_open(code, date, hour)\n","lineDetails":[{"lines":["1-54","68-96","101-163","169-192","207-209","240-305"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"},{"lines":["55-67","97-100","193-206","210-239"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"2e1ba1ac-3013-4fbb-8557-e44020720c88"},{"lines":["164-168"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"7443cdbf-7a38-4af2-b23f-53ce5c6bf833"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/tools/backfill_index_kline.py","aiAddedLines":["1-90"],"aiDeletedLines":[],"aiModifiedContent":"#!/usr/bin/env python3\n\"\"\"[Task #10] index_kline 缺行回补: 以 stock_kline 交易日为基准,\n补齐 sh.000001 缺失日期行(BaoStock拉取, 失败重试)。\n\nsz.399001 裁定: 全仓库代码 0 处引用(grep核验), 明确废弃不再更新,\n保留2020-01-02~2026-05-14存量行不删除。\n\n用法: python3 tools/backfill_index_kline.py\n\"\"\"\nimport math\nimport sqlite3\nimport time\n\nimport baostock as bs\n\nDB = '/home/AIWealth/data/stocks.db'\n\n\ndef safe_float(val):\n    if val is None or val == '':\n        return None\n    try:\n        v = float(val)\n        return None if math.isnan(v) or math.isinf(v) else v\n    except (ValueError, TypeError):\n        return None\n\n\ndef calc_rate(price, preclose):\n    if price is None or preclose is None or preclose == 0:\n        return None\n    return round((price / preclose - 1) * 100, 4)\n\n\ndef main():\n    conn = sqlite3.connect(DB)\n    missing = [r[0] for r in conn.execute(\n        \"SELECT DISTINCT s.date FROM stock_kline s \"\n        \"LEFT JOIN index_kline i ON i.date=s.date AND i.code='sh.000001' \"\n        \"WHERE i.date IS NULL ORDER BY s.date\")]\n    if not missing:\n        print(\"index_kline sh.000001 无缺行\")\n        conn.close()\n        return\n    print(f\"缺行: {missing}\")\n\n    lg = bs.login()\n    if lg.error_code != '0':\n        print(f\"BaoStock登录失败: {lg.error_msg}\")\n        conn.close()\n        return\n\n    filled = 0\n    for d in missing:\n        for attempt in range(3):  # 限流/断线重试\n            rs = bs.query_history_k_data_plus(\n                'sh.000001',\n                'date,code,open,high,low,close,preclose,volume,amount',\n                start_date=d, end_date=d, frequency='d')\n            rows = []\n            while rs.error_code == '0' and rs.next():\n                rows.append(rs.get_row_data())\n            if rows:\n                break\n            print(f\"{d}: 第{attempt + 1}次拉取为空/失败, 重试...\")\n            time.sleep(2)\n        if not rows:\n            print(f\"{d}: 3次重试仍失败, 跳过\")\n            continue\n        row = rows[0]\n        o, h, l, c, pre = [safe_float(x) for x in row[2:7]]\n        vol, amt = safe_float(row[7]), safe_float(row[8])\n        vals = [row[0], row[1], '上证综指', o, h, l, c, pre, vol, amt,\n                calc_rate(o, pre), calc_rate(h, pre),\n                calc_rate(l, pre), calc_rate(c, pre)]\n        vals.extend([None] * 40)  # hour1-4 占位\n        vals.append(None)         # red_ratio 由 update_red_ratio.py 生成\n        conn.execute(\n            'INSERT OR REPLACE INTO index_kline VALUES ('\n            + ','.join(['?'] * 55) + ')', vals)\n        conn.commit()  # 短事务, 避免长锁\n        filled += 1\n        print(f\"{d}: close={c} 已补\")\n    bs.logout()\n    conn.close()\n    print(f\"完成: 补{filled}/{len(missing)}行\")\n\n\nif __name__ == '__main__':\n    main()\n","lineDetails":[{"lines":["1-90"],"type":"added","brand":"qoder","product":"IDE","scenario":"agent","sessionId":"3970bf21-7ca1-42f7-bc00-5566524088e4","userQueryBusinessId":"413f34a4-7ccf-4eba-a838-0b33cd39322d"}],"gitRoot":"/home/AIWealth"}
{"filePath":"/home/AIWealth/strategies/firstboard_low_open_dip.py","aiAddedLines":["1-302"],"aiDeletedLines":[],"aiModifiedContent":"\"\"\"首板次日低开低吸策略 (FB-A) - Task#9 引擎精测。\n\n形态(来自 Task#7 全维度网格复审, 研究口径见 data/realtime/task7_refine.txt):\n  昨日(D0)首板涨停(连板数==1, 非一字) → 今日(D1)竞价低开 -4% \u003c= open_rate \u003c -2%\n  → 昨日红盘占比 red_ratio \u003e= 60(强势日错杀) → H1_open 买入\n  → 退出 tp+6% / sl-10%, 最长5个监控日(D6收盘兜底)\n排序: D0换手升序(低换手=筹码未松动优先) — task7_top1_probe:\n  top1 +1.50%/日 2021-2026 六年全正; 注意换手降序方向则失效(-0.31%)。\n\n研究口径(池子级 tp6sl-10, 毛收益): n=1574 胜率58.4% 均值+0.73% 5正1平\n引擎精测(2021-01~2026-07-15, slot=1, 含成本): CAGR +72.53% | MDD 44.81%\n  | 219笔 胜率65.75% +1.62%/笔 | 唯一负年 2023 -7.9% → 观察池第一顺位\nTask#12 干净数据+全区间重跑(至2026-07-23): CAGR +64.85%, 2026 +16.9→-7.8\n  (数据修复+区间延长), 2021-2025 分年不变; 卖点前移两变体(d2c/h1)引擎\n  精测均劣于本对照(+56.8%/+61.3%), FB-A **保持现行 tpsl_d6 口径不动**——\n  粗测代理曾示 d2c 更优(+86%), 引擎证伪: 变体笔数增加(226→332/447)但\n  均收益稀释(+1.47→+0.92/+0.74%/笔), slot=1 路径依赖下 2021/2024/2026\n  转弱, MDD 还升(44.8→45.0/55.7)。只信引擎数据。\nred_ratio 权威源=index_kline(sh.000001) red_ratio 列, 2026-07-23 前已补齐\n(Task#10 Robin 修复); 缺失日显式跳过不买入作为防御, 不引入未来数据。\n\nTask#12 卖点前移(exit_mode 参数化, 买入逻辑不动):\n  tpsl_d6   现行对照: tp6/sl-10 挂单监控 D2..D6, D6收盘兜底\n  tpsl_h1   挂单仅监控 D2 H1, 未触发 H1收盘强平 (D2起slot当小时可复用)\n  d2h1_open / d2h1_close / trail_h1  同 B2-A 语义\n依据: Lee时段结构(H1唯一系统性正时段) x Task#9归因(hold长占slot致信号捕获减半)。\n\"\"\"\ni